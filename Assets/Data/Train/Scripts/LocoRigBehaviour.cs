using UnityEngine;

public class LocoRigBehaviour : WagonRigBehaviour
{
    /*
        [SerializeField]
        protected float speed = 10f;

        public override void UpdateRig()
        {

            if (splineServiceBehaviour.TryToGetTransformDataFrom(WagonTransform.position, out var newPos, out var quaternion))
            {
                WagonTransform.position = newPos;
                WagonTransform.rotation = quaternion;
                rb.velocity = transform.forward * speed;
            }
        }*/

}
