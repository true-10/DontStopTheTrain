using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace DontStopTheTrain.Events.DataManagers
{/*
    public interface IEventsManager
    {
        void Init();
        IEvent GetEvent(int id);
    }


    public class WagonEventsDataManager : MonoBehaviour, IEventsManager
    {
        [SerializeField] private WagonEventsDataScriptableObject eventsData ;

        private List<IWagonEvent> events = new();
        public void Init()
        {
            foreach (var data in eventsData.events)
            {
                events.Add(data);
            }
        }

        public IEvent GetEvent(int id)
        {
            throw new System.NotImplementedException();
        }

        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }*/
}
